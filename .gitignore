# Local run state — machine specific, not part of the workshop
.run/
.venv/
setup-log.txt
*.log

# Hands-on 1 training outputs (merged model, GGUF, adapter)
hands-on-1-finetune/output/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Stale SQLite sidecars: shipping these alongside the fallback index can
# corrupt a restore
hands-on-2-rag/index/fallback/*.sqlite-wal
hands-on-2-rag/index/fallback/*.sqlite-shm
